import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Notifications",
  description: "Notifications",
}

const page = () => {
  return (
    <div>page</div>
  )
}

export default page