import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Add Notification',
  description: 'Add Notification',
}

const page = () => {
  return <div>add notification</div>
}

export default page
